spring:
  application:
    name: ReservationSite
  flyway:
    baseline-on-migrate: true
    baseline-version: 1
    enabled: false
  datasource:
    url: jdbc:postgresql://ep-withered-rain-ahzxwqzw-pooler.c-3.us-east-1.aws.neon.tech/neondb?&sslmode=require&channelBinding=require
    username: neondb_owner
    password: npg_b2zXONwLm5ME
    hikari:
      pool-name: CinemaPool
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 3000
      idle-timeout: 300000
      max-lifetime: 1800000
      validation-timeout: 1000
      keepalive-time: 300000
      initialization-fail-timeout: 0
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
    defer-datasource-initialization: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 100MB
cloudflare:
  r2:
    access-key: ${R2_ACCESS_KEY}
    secret-key: ${R2_SECRET_KEY}
    endpoint: ${R2_ENDPOINT}
    bucket: ${R2_BUCKET_NAME}
    public-url: ${R2_PUBLIC_ADDRESS}
app:
  jwt:
    secret: ${JWT_SECRET}
    expiration-time: ${JWT_TIME}

